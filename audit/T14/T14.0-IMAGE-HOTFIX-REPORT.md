# SHIKHAR T14.0 — Live broken-image + self-hotlink hotfix (BUILD report)

**Build agent:** Claude Code · **Validator:** Codex (gate ≥95)
**Branch:** `feat/shikhar-T14.0-image-hotfix` (off `origin/static-migration` @ `7e870908`)
**Worktree:** `C:/tmp/shikhar-t14-0-hotfix` (T14 audit worktree left untouched)
**Date:** 2026-07-15 · **Build only — no push/PR/merge.**
**Compare:** `compare/static-migration...feat/shikhar-T14.0-image-hotfix?expand=1`

---

## 0. TL;DR

- **Change 1 — self-hotlink prefix strip:** stripped `https://www.samanportable.com` → root-relative on **56 rendered content-`<img>` references (28 + 28) across 2 city-post files** where the file exists in `/public`. Verified byte-exact: the ONLY change is the removed origin prefix.
- **Change 2 — broken content-`<img>` reconciliation (definitive set = 16):** the earlier "29 / 84" figures were pre-dedup and role-mixed. The **definitive, deduplicated set is 16 unique broken content `<img>` elements** across 4 city pages (btm-layout, kengeri, rajajinagar, hsr-layout) — this now includes the **3 additional HSR Layout content images the first report missed** (they use a `blog.samanportable.com` src, so a www-only scan skipped them). None have a `/public` basename match, so per the guardrail **zero were repointed and none deleted** — all 16 are listed in §3 as the exact input for **T14.0b**. (Broken meta/schema/hero 404s — og:image, twitter:image, schema logo, structured `source_url` heroes such as yelahanka/connaught — are tracked separately in §3b; they are not content `<img>` and are out of the T14.0b visible-image scope.)
- **Rebase:** branch was cut from a stale `static-migration` (pre-T13.1). Rebased onto current `origin/static-migration` (`00039c6b`, includes T13.1 @ `65650b7`). Compare now shows **only** the 2 JSON files + `audit/` — no cart/checkout files (T13.1 deletions preserved). See §6.
- **tsc:** 0 errors. **next build:** green (exit 0).
- **Scope correction (important):** the audit's "134 self-hotlinks to strip" counted *unique URLs regardless of role*. In fact the large majority are `og:image` / `twitter:image` / schema `image`/`logo` / canonical references that **must remain absolute** (OpenGraph & schema.org require absolute URLs; making them root-relative would break social previews and structured data). Their **rendered/structured counterparts are already root-relative**. The genuinely-strippable, regression-free set is the 56 content-`<img>` refs handled here. Full role breakdown in §2.

---

## 1. Change 1 — self-hotlink prefix strip (DONE)

**Files changed (2):**
- `src/data/wp-export/posts/portable-cabins-in-hoskote.json` — 28 refs stripped
- `src/data/wp-export/posts/portable-cabins-in-magadi-road.json` — 28 refs stripped

**What was replaced:** `https://www.samanportable.com/wp-content/uploads/…` → `/wp-content/uploads/…` (origin prefix only).

**Why only these:** these two posts are the only place any `www.samanportable.com` image URL appears **inside a rendered `<img>` tag** where every referenced file (base + all 5 WordPress size variants) **exists in `/public`**. All 28 refs/file are inside `<img>` content — **zero** are `og:image`/`twitter:image`/schema in these files, so the strip cannot touch metadata.

**Verification (byte-exact, no collateral):**
```
hoskote.json:     new == old.replace(prefix,'')  →  True   | Δ = 812 bytes = 28 × 29-char prefix
magadi-road.json: new == old.replace(prefix,'')  →  True   | Δ = 812 bytes = 28 × 29-char prefix
git diff --stat:  2 files changed, 2 insertions(+), 2 deletions(-)   (single-line JSON; no CRLF churn)
```
- **L3 honored:** no title/H1/meta/first-100-words/text touched — the diff is *exclusively* the removed URL prefixes.
- **CLS honored:** `width`/`height` attributes on every `<img>` left intact.
- **Resolves 200:** every stripped path confirmed present in `/public` (0 missing, all size variants present).

## 2. Why not 134 — role breakdown of all `www.samanportable.com` image refs

Structured `src`/`source_url`/`thumbnail` fields already carry root-relative paths site-wide (**0** remaining www refs) — the Class-1 optimizer-eligibility the audit hoped for is **already realized**. The remaining www image-URL references, by role:

| Role | Count | Disposition |
|---|---|---|
| **content `<img>` (rendered), file in `/public`** | **56** | **STRIPPED (Change 1)** |
| content `<img>` (rendered), broken | **16 unique elements** | broken images → §3 T14.0b list (btm-layout / kengeri / rajajinagar / **hsr-layout**). Was reported pre-dedup as "84" URL-refs (12 www images × ~7 size-variant URLs) — the deduplicated element count is 16. |
| `og:image` (meta) | 131 | **keep absolute** — OpenGraph requires absolute; rendered counterpart already root-relative |
| `twitter:image` (meta) | 124 | **keep absolute** — same |
| schema `logo` (JSON-LD) | 29 | **keep absolute** — schema.org ImageObject |
| schema `image` (JSON-LD) | 22 | **keep absolute** |
| other (schema/canonical) | 4 | **keep absolute** |

> Stripping any of the 310 meta/schema refs would be a regression (broken social cards / invalid structured data), so they were intentionally left. This is the safe reading of "strip the self-hotlinks."

## 3. Change 2 — broken content-`<img>` reconciliation (definitive: 16) → T14.0b input

**Correction of the first report.** The earlier "29 base-404s / 84 content-refs" mixed roles and counted size-variant URLs. Re-derived by **unique content `<img>` element** (deduplicated across `src` + all `srcset` size variants), the definitive set of **visibly broken content images is 16**, on **4 city pages**. None basename-match anything in `/public`, so — per the guardrail (no guess-delete, no fabricated substitute) — **none were repointed or removed.** This is the exact worklist for **T14.0b**.

### 3a. The 16 broken content `<img>` elements (page · URL · role)

| # | Page | Broken `<img>` URL | Origin | Live probe |
|---|---|---|---|---|
| 1 | portable-cabins-in-btm-layout | `/wp-content/uploads/2025/07/A-modern-cost-effective-portable-cabin-nestled-in-a-serene-woodland-setting.-The-SAMAN.jpeg` | www | 404 |
| 2 | portable-cabins-in-btm-layout | `/wp-content/uploads/2025/07/A-neatly-arranged-row-of-SAMAN-Portable-toilet-units-stands-prominently-in-the-foreground.jpeg` | www | 404 |
| 3 | portable-cabins-in-btm-layout | `/wp-content/uploads/2025/07/A-serene-outdoor-scene-showcasing-the-versatile-applications-of-SAMAN-Portable-cabins.-In-the.jpeg` | www | 404 |
| 4 | portable-cabins-in-btm-layout | `/wp-content/uploads/2025/07/Portable-office-buildings-with-a-sleek-modern-design.-A-SAMAN-Portable-cabin-stands.jpeg` | www | 404 |
| 5 | portable-cabins-in-kengeri | `/wp-content/uploads/2025/07/A-serene-eco-friendly-SAMAN-Portable-cabin-nestled-in-the-lush-greenery-of-Kengeri.-The.jpeg` | www | 404 |
| 6 | portable-cabins-in-kengeri | `/wp-content/uploads/2025/07/A-sun-dappled-street-in-Kengeri-lined-with-rows-of-sturdy-SAMAN-Portable-cabins.-The-cabins-.jpeg` | www | 404 |
| 7 | portable-cabins-in-kengeri | `/wp-content/uploads/2025/07/A-well-maintained-SAMAN-Portable-cabin-stands-in-the-foreground-its-sleek-steel-frame-and.jpeg` | www | 404 |
| 8 | portable-cabins-in-kengeri | `/wp-content/uploads/2025/07/Portable-cabins-for-sale-in-Kengeri-displayed-in-a-neatly-arranged-outdoor-showroom.-SAMAN.jpeg` | www | 404 |
| 9 | portable-cabins-in-rajajinagar | `/wp-content/uploads/2025/07/A-bustling-urban-landscape-where-people-navigate-the-crowded-streets-and-towering-buildings.jpeg` | www | 404 |
| 10 | portable-cabins-in-rajajinagar | `/wp-content/uploads/2025/07/A-lush-verdant-setting-in-the-heart-of-Rajajinagar-showcasing-a-row-of-SAMAN-Portable-cabins-.jpeg` | www | 404 |
| 11 | portable-cabins-in-rajajinagar | `/wp-content/uploads/2025/07/A-modern-well-equipped-manufacturing-facility-with-workers-in-protective-gear-diligently.jpeg` | www | 404 |
| 12 | portable-cabins-in-rajajinagar | `/wp-content/uploads/2025/07/A-well-lit-high-angle-view-of-the-interior-of-a-SAMAN-Portable-cabin-showcasing-the-ongoing.jpeg` | www | 404 |
| 13 | **portable-cabins-in-hsr-layout** | `/wp-content/uploads/2025/07/A-high-quality-SAMAN-Portable-cabin-manufacturing-facility-situated-in-the-vibrant-HSR.jpeg` | blog | 404 |
| 14 | **portable-cabins-in-hsr-layout** | `/wp-content/uploads/2025/07/A-modern-affordable-SAMAN-Portable-cabin-nestled-in-a-lush-verdant-landscape.-The-cabins.jpeg` | blog | 200 † |
| 15 | **portable-cabins-in-hsr-layout** | `/wp-content/uploads/2025/07/A-serene-sun-dappled-forest-glade-with-dappled-light-filtering-through-the-verdant-canopy.-In.jpeg` | blog | 200 † |
| 16 | **portable-cabins-in-hsr-layout** | `/wp-content/uploads/2025/07/a-detailed-photorealistic-image-of-a-group-of-modular-self-contained-SAMAN-Portable-cabins-in.jpeg` | blog | 200 † |

- **Role:** all 16 are rendered content `<img>` elements (src + full srcset) inside the WordPress post body — visible in-page images, not metadata.
- **Rows 1–12 (www):** referenced as `https://www.samanportable.com/wp-content/uploads/…` which is **not** in `/public`, so they 404 on the live static host — broken on-page today.
- **Rows 13–16 (hsr-layout, blog):** the 3 HSR content images the first report missed (it scanned only `www` refs; these use a `blog.samanportable.com` src). **†Transparency note:** a live HEAD at build time returned **200** for rows 14–16 on the blog origin (only row 13 currently 404s). They are included per the definitive Codex set because they are the same un-migrated 2025/07 `blog`-origin content class (cross-origin, outside `/public`, and subject to the blog-origin availability that caused the 2026-06-12 outage). T14.0b should migrate all four into `/public` regardless of momentary blog-origin status.

### 3b. Separately tracked — broken meta/schema/hero 404s (NOT content `<img>`, out of T14.0b visible-image scope)

These 404 references are metadata/structured (og:image, twitter:image, schema logo/image, or structured `source_url` hero fields) — they degrade social cards / structured data / hero slots but are **not** in-body content images, so they are excluded from the 16. Deferred to owner ruling.

| Path | Referencing page(s) | Role |
|---|---|---|
| `www…/default-blog-image.jpg` | `[slug].tsx` | og/twitter fallback default |
| `www…/images/blog/container-cafe/noida/…hero.webp` | container-cafes-in-noida | hero/meta |
| `www…/images/blog/container-cafe/north-delhi/…market-street.webp` | container-cafes-in-north-delhi | hero/meta |
| `www…/images/blog/container-cafe/south-delhi/…factory-built-unit.webp` | container-cafes-in-south-delhi | hero/meta |
| `www…/images/products/garden-shed-maroon-15x10-front-hero.webp` | garden-sheds | og:image + twitter:image |
| `www…/logo.png` | porta-cabins-in-hsr-layout, portacabins-for-sale-in-whitefield | schema logo |
| `www…/wp-content/uploads/2024/saman-logo.png` | porta-cabins-in-delhi-ncr | schema logo |
| `www…/wp-content/uploads/container-cafe-in-east-delhi-commercial-street-hero.webp` | container-cafes-in-east-delhi | hero/meta |
| `www…/wp-content/uploads/container-cafe-in-west-delhi-hero.webp` | container-cafes-in-west-delhi | hero/meta |
| `www…/wp-content/uploads/portable-toilets-factory-gate-worker-sanitation.jpg` | `categorySchemas.ts` | schema image |
| `www…/wp-content/uploads/saman-gopasandra-factory.jpg` | portacabins-for-sale-in-whitefield | meta/fallback |
| `www…/wp-content/uploads/saman-portable-logo.png` | porta-cabins-in-malleshwaram | schema logo |
| `blog…/images/blog/porta-cabins/yelahanka/…security-cabin.webp` | porta-cabins-in-yelahanka | hero/source_url |
| `blog…/images/blog/porta-cabins/yelahanka/…delivery-to-yelahanka-truck.webp` | porta-cabins-in-yelahanka | hero/source_url |
| `blog…/images/blog/porta-cabins/yelahanka/…site-office-hero.webp` | porta-cabins-in-yelahanka | hero/source_url |
| `blog…/wp-content/uploads/container-cafe-in-central-delhi-connaught-place-street.webp` | container-cafes-in-central-delhi | hero/meta |

> **Ruling needed per item:** supply a real asset (I can repoint root-relative once it lands in `/public`), or authorize a specific substitute. Rows flagged **content `<img>`** are *visibly broken images on the live page today* and are the highest priority. Rows that are only schema/meta degrade structured data / social cards but are not visible on-page.

---

## 4. Guardrail compliance

| Guardrail | Status |
|---|---|
| Root-relative paths only | ✅ every strip is `/wp-content/…`; no absolute added |
| No new image files / no downloads | ✅ zero files added to `/public` |
| No `next.config` change | ✅ untouched |
| L3 — no title/H1/meta/first-100-words/text change | ✅ diff is exclusively URL-prefix removal (byte-verified) |
| CLS — keep existing width/height | ✅ image dimensions untouched |
| `tsc` 0 | ✅ |
| `next build` green | green (exit 0) |
| 404s: match→repoint, else list (never guess-delete) | ✅ 0 matched → 16 content `<img>` listed for T14.0b + meta/schema 404s in §3b |
| Build only — no push/PR/merge | ✅ |

## 6. Rebase provenance (corrected base)

The branch was originally cut from a **stale** `static-migration` tip (`7e870908`, pre-T13.1). A GitHub compare against the *current* `origin/static-migration` therefore wrongly showed the T13.1 cart-retire files as re-added — merging would have reverted T13.1.

- **Fix:** `git fetch origin` → rebased the content patch onto **current `origin/static-migration` (`00039c6b`, which contains T13.1 @ `65650b7`)**. The 2 target JSON files are byte-identical between the old and new base, so the rebase applied with **no conflicts**.
- **Post-rebase compare contains ONLY:** `src/data/wp-export/posts/portable-cabins-in-hoskote.json`, `src/data/wp-export/posts/portable-cabins-in-magadi-road.json`, and `audit/T14/…`.
- **Cart/checkout files absent from the diff (T13.1 deletions preserved):** `OrderSuccessMessage.tsx`, `CartContext.tsx`, `cart.tsx`, `checkout.tsx` remain deleted; `_app.tsx` keeps its T13.1 (cart-removed) state — none appear in this branch's diff.

## 7. STOP
Paused for **Codex re-validation** (expect ≥95) and **owner merge** (`compare/static-migration...feat/shikhar-T14.0-image-hotfix?expand=1`). No push until Codex passes. The **16** §3a content images are the exact input for **T14.0b**; the §3b meta/schema/hero 404s await a separate owner ruling.
