# SHIKHAR T14.0b — Broken content-image cleanup (BUILD report)

**Build agent:** Claude Code · **Validator:** Codex (gate ≥95)
**Branch:** `feat/shikhar-T14.0b-broken-img-cleanup-v2`
**Worktree:** `C:/tmp/shikhar-t14-0b-cleanup-v2`
**Base:** current `origin/static-migration` @ `55a95d1f` (includes **T14.0 @ merge PR #35**)
**Date:** 2026-07-15 · **Build only — no push/PR/merge.**
**Compare:** `compare/static-migration...feat/shikhar-T14.0b-broken-img-cleanup-v2?expand=1`

---

## 0. TL;DR

- Input: the **16** broken content-`<img>` worklist from the T14.0 report (btm ×4, kengeri ×4, rajajinagar ×4 = 12 www; HSR ×4 blog).
- Re-verified every ref at build time (live HEAD + `/public`). **13 confirmed 404 → removed** (12 www + 1 HSR). **3 HSR blog images returned 200 → kept and flagged** (as expected — they are working images for the later blog-image migration).
- Each broken `<img>` sat alone in a `<p><img/></p>` wrapper → removed the whole empty wrapper. **No `<figure>`/`<figcaption>`/`<div>` wrappers and no captions exist in these files**, so nothing orphaned.
- **Body text byte-for-word identical** before/after (proven per file); **no L3 zone hit** (nearest broken img is 304 words into the body); **CLS 0.000** (build-time removal, no runtime shift).
- **tsc:** 0. **next build:** green (exit 0).
- Independent cross-check: result matches the earlier interrupted attempt exactly (which was on a stale pre-T14.0 base; this redo is on the correct base — see §5).

---

## 1. Per-ref live re-verification + action (all 16)

Removed = `<p><img/></p>` element deleted. Status = live HEAD at build time.

| # | Page | Image (base name) | Origin | Live status | /public | Action |
|---|---|---|---|---|---|---|
| 1 | btm-layout | A-modern-cost-effective-…woodland-setting.-The-SAMAN | www | **404** | no | **REMOVED** |
| 2 | btm-layout | A-serene-outdoor-scene-…cabins.-In-the | www | **404** | no | **REMOVED** |
| 3 | btm-layout | Portable-office-buildings-…cabin-stands | www | **404** | no | **REMOVED** |
| 4 | btm-layout | A-neatly-arranged-row-of-…toilet-units-…foreground | www | **404** | no | **REMOVED** |
| 5 | kengeri | A-sun-dappled-street-in-Kengeri-…cabins- | www | **404** | no | **REMOVED** |
| 6 | kengeri | Portable-cabins-for-sale-in-Kengeri-…showroom.-SAMAN | www | **404** | no | **REMOVED** |
| 7 | kengeri | A-serene-eco-friendly-…greenery-of-Kengeri.-The | www | **404** | no | **REMOVED** |
| 8 | kengeri | A-well-maintained-…steel-frame-and | www | **404** | no | **REMOVED** |
| 9 | rajajinagar | A-bustling-urban-landscape-…towering-buildings | www | **404** | no | **REMOVED** |
| 10 | rajajinagar | A-lush-verdant-setting-…Rajajinagar-…cabins- | www | **404** | no | **REMOVED** |
| 11 | rajajinagar | A-modern-well-equipped-manufacturing-facility-…diligently | www | **404** | no | **REMOVED** |
| 12 | rajajinagar | A-well-lit-high-angle-view-…the-ongoing | www | **404** | no | **REMOVED** |
| 13 | hsr-layout | A-high-quality-…manufacturing-facility-…HSR | blog | **404** | no | **REMOVED** |
| 14 | hsr-layout | A-modern-affordable-…lush-verdant-landscape.-The-cabins | blog | **200** | no | **KEPT (flag)** |
| 15 | hsr-layout | A-serene-sun-dappled-forest-glade-…verdant-canopy.-In | blog | **200** | no | **KEPT (flag)** |
| 16 | hsr-layout | a-detailed-photorealistic-…self-contained-…cabins-in | blog | **200** | no | **KEPT (flag)** |

**Removed: 13** (expected ~13 ✓). **Kept 200: 3.**

## 2. Wrapper / caption / L3 handling

- **Wrapper:** every one of the 13 broken images was the sole child of a `<p>` (`…</p><p><img …/></p><h2>…`). Removed the whole `<p><img/></p>` unit (+ its one trailing source newline). **0 empty `<p></p>` remain** (verified).
- **Captions:** the 4 files contain **no `<figure>` and no `<figcaption>`** — nothing orphaned, nothing to flag.
- **L3 (first-100-words):** the earliest broken image is **304 words** into the body (rajajinagar #9); every other is further in. **None fall in the first-100-words zone** — no L3 flags, no auto-remove suppressed.

## 3. Safety verification

| Check | Result |
|---|---|
| Removed count = 13 (12 www + 1 HSR 404) | ✅ btm 4→0, kengeri 4→0, rajajinagar 4→0, hsr 4→3 |
| 3 HSR **200** images kept intact (src + full srcset) | ✅ each present, 7 URLs each |
| No remaining `<img>` points at any removed (404) marker | ✅ 0 |
| Body text word-sequence identical (tags stripped) | ✅ btm 4280=4280 · kengeri 4867=4867 · rajajinagar 3769=3769 · hsr 5163=5163 |
| L3 — no title/H1/meta/first-100-words/text change | ✅ only `<p><img/></p>` units removed |
| Only image ELEMENTS removed (no body text) | ✅ proven per file |
| CLS 0.000 | ✅ build-time removal; no runtime shift (broken imgs no longer reserve boxes) |
| Never removed/altered a 200 ref | ✅ 3 HSR 200s untouched |
| `tsc` 0 | ✅ |
| `next build` green | green (exit 0) |
| Build only — no push/PR/merge | ✅ |

**Note (out of scope, unchanged):** these broken URLs appear **only** as content `<img>` in these posts — none are used as this post's `og:image`/`twitter:image` (residual-marker scan = 0 after removal). The separate meta/schema/hero 404s from the T14.0 report (§3b there: yelahanka `source_url` heroes, connaught, og/twitter/logo) are **not** touched here; they remain for their own owner ruling.

## 4. Flags for Fable 5

- **3 HSR blog images (rows 14–16) currently return 200** and were **kept**. They are cross-origin (`blog.samanportable.com`) un-migrated 2025/07 content images — leave for the later blog→/public migration (T14 Part B). No action needed now; flagged so they are not mistaken for missed removals.
- **No orphaned captions.** (None existed.)
- **No L3-zone broken images.** (None existed.)

## 5. Base / worktree provenance

The branch name `feat/shikhar-T14.0b-broken-img-cleanup` already existed from an **interrupted prior attempt on a stale base** (`00039c6b`, pre-T14.0) with uncommitted work. Per the one-worktree-per-agent rule, that worktree was **left untouched** (its WIP preserved), and this work was done in a **fresh worktree + branch `…-v2` off the correct base `55a95d1f`** (which includes T14.0). The independent redo matches the prior attempt's removal set exactly, on the correct base so the compare does **not** revert T14.0.

## 6. STOP
Paused for **Codex validation** (expect ≥95) and **owner merge** (`compare/static-migration...feat/shikhar-T14.0b-broken-img-cleanup-v2?expand=1`). No push until Codex passes.
