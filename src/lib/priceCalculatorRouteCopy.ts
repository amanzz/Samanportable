/**
 * Approved, L3-locked copy for the /portable-cabin-price-calculator route.
 * ONE definition site, for the same reason GST has one: a string that renders in
 * two places must not be written in two places.
 *
 * Authority: the H1 already published on that route. CALC-L7 §2.3 needs a label
 * for the header's calculator entry point, and this programme's rule is that
 * reusing a string the page already publishes is reuse, not invention. No new
 * customer-facing copy was written for the header - a shorter menu label would
 * be new copy and needs SAMAN's approval, so it was not composed.
 *
 * Do not edit this string. It is the page's H1 and it is L3-locked.
 */
export const PRICE_CALCULATOR_H1 = 'Customize Your Portable Cabin & Calculate Price';

/** The route the header entry point and the homepage strip both point at. */
export const PRICE_CALCULATOR_ROUTE = '/portable-cabin-price-calculator';
