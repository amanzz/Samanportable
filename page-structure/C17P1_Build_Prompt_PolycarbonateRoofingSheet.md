**SAVE TO (auto-written to both checkouts):** `page-structure\C17P1_Build_Prompt_PolycarbonateRoofingSheet.md`

# BUILD PROMPT — /product/roofing-sheet/polycarbonate-roofing-sheet (LOCAL ONLY, preview = HARD GATE)
**Destination tool: build agent (Codex role). Zero content decisions. Read first: Law file Part A (L1–L9/R12) + Rulings R1–R15 + content-drafts\C17P1_PolycarbonateRoofingSheet_DRAFT.md.**

1. **Template:** exact L1 product design proven on the roofing hub and metal page — gallery 5×1:1, dual-zone quotation card under gallery, summary box (chip ROOFING SHEETS / H1 / green "From ₹66 / sq ft" / grey subline "4mm multiwall base spec · ex-GST · final price at quotation" / intro / icon grid), SKU **SP-C17-PCR-SUB-2026**, four tabs, R13 rail (5 thumbnail cards: Roofing Sheet hub · Metal Roofing Sheet **[verify /product/roofing-sheet/metal-roofing-sheet returns 200 at build time; if not live, substitute PIR Panel]** · PUF Panel · Sandwich Panel · EPS Panel — thumbnail + title + chip, no blurbs, no carts). Mobile order: gallery → CTA → description. No FOUC/CLS regressions (image dimensions fixed).
2. **WC-first (R8):** create WC product Polycarbonate Roofing Sheet (slug polycarbonate-roofing-sheet, SKU, price 66, category Roofing Sheets 3950) Draft + No Index; wire wc_review_product_id; sync JSON mirror with real id.
3. **R14 + R15 in the SAME commit:** canonical-map entry — /product/roofing-sheets/polycarbonate-roofing-sheet (and any category-nested variant) 301 → canonical; sitemap emits canonical only; category card links canonical. Register the product in the master product-listing feed (R15) — as Draft it correctly stays OUT of the /product grid, dropdown counts and category listings until published; do not "fix" that.
4. **Content:** Description tab, Specifications tab (2 tables + 3 diagrams, no price columns), 9 FAQs (FAQPage JSON verbatim, slug allowlisted), CTAs, cert strip, meta/canonical (4-level breadcrumb per draft) — ALL verbatim. Schema: Product + AggregateOffer lowPrice 66 offerCount 7 + Breadcrumb + FAQPage; no ratings.
5. **Images:** 13 PNGs expected in panel-images\polycarbonate-roofing-sheet\ (names in draft). WebP 1:1 1200×1200 / 16:9 1200×675, ≤120 KB, alts verbatim. Missing = STOP + list names.
6. **Links:** wire the 4 live anchors from the draft's table; apply the CONDITIONAL metal-sibling rule exactly as written (wire "our metal roofing sheet range" in the daylight-strips section ONLY if the metal page is live 200, else leave plain text); log the 2 new anchors (+ the metal one if wired) in the C17 anchor register (unique program-wide). PVC/price stay plain text.
7. **Verify:** dev 200, one H1, tabs, ₹66 + SKU + grid, tsc/lint, `62009` zero, review-submit vs new WC id, category-nested URL 301s locally, sitemap single entry, full HTML in view-source (R11), scoped git status (per-path staging, NO git add -A).
8. **STOP:** preview URL + screenshots (summary, three-construction table, spec tab diagrams, rail, mobile) → WAIT for SAMAN's written approval → then standard deploy (feat branch → PR link to SAMAN → main → static-migration, union-merge shared files) → SHA verify → DO wait (never re-push on lag) → live verify (incl. duplicate 301 + sitemap grep + Rich Results offers + R15 listing check once published) → report. GSC = SAMAN, manually, after live verification.
