# RULING — C-01 RETIREMENT & REDIRECT MAP · Fable 5 · 26 Jul 2026
### SAMAN: "the weakest is retired, redirect it, remove all trouble." Decided on 16-month GSC data, not opinion.

**22 URLs retired. 826 clicks consolidated onto winners. No term abandoned — every retired page's keyword survives as a supporting keyword on the page that absorbs it.**

---

## 1. THE PAIR YOU ASKED ABOUT — DECIDED

| URL | Clicks | Impressions | Verdict |
|---|---|---|---|
| `/product/porta-cabins/porta-cabin-office` | **48** | **57,940** | **WINNER — keep** |
| `/product/porta-cabins/portacabin-office` | 22 | 17,350 | **RETIRED — 301 → porta-cabin-office** |

Porta Cabin Office wins on both measures by better than 2:1 and 3:1. **`portacabin-office` is retired.**

This also cancels the correction I raised in Drop 2 — the page whose grade contradicted its price no longer exists. **C-01 is now 13 pages, not 14.** Yesterday's right-to-exist block and PDF entry for `portacabin-office` are withdrawn; nothing else in that pack changes.

**The term "portacabin office" is NOT abandoned.** Per L12 it becomes a supporting keyword of `/product/porta-cabins/porta-cabin-office`, carried in body copy and in one H3 on that page. You keep the term; you stop having two pages fight over it.

---

## 2. EVERY OTHER TROUBLEMAKER IN C-01 — the full clear-out

### 2.1 Stray legacy `/product/<slug>` paths — 7 URLs, pure duplicates
Each of these duplicates a canonical `/product/porta-cabins/<slug>` page. Their impression counts (35–252) versus the canonicals' (2,250–113,326) prove Google already treats them as duplicates and suppresses them; their clicks are brand and direct traffic that a 301 keeps.

| Retire | 301 → | Clicks lost to the stray |
|---|---|---|
| `/product/luxury-porta-cabin` | `/product/porta-cabins/luxury-porta-cabin` | 28 |
| `/product/low-cost-porta-cabin` | `/product/porta-cabins/low-cost-porta-cabin` | 17 |
| `/product/buy-porta-cabins` | `/product/porta-cabins/buy-porta-cabins` | 14 |
| `/product/porta-cabin-office` | `/product/porta-cabins/porta-cabin-office` | 13 |
| `/product/porta-cabin-shop` | `/product/porta-cabins/porta-cabin-shop` | 11 |
| `/product/prefabricated-porta-cabin` | `/product/porta-cabins/prefabricated-porta-cabin` | 6 |
| `/product/steel-porta-cabin` | `/product/porta-cabins/steel-porta-cabin` | 5 |

### 2.2 The MS Porta Cabin wrong-path problem — and a law amendment
| URL | Clicks | Impressions |
|---|---|---|
| `/product/porta-cabin/ms-porta-cabin` *(singular path — a cluster that does not exist)* | 32 | **19,020** |
| `/product/porta-cabins/ms-porta-cabin` *(canonical)* | 21 | 2,250 |

**The wrong path is out-earning the right one 8.5:1 on impressions.** Google has picked the orphan as the real page.

My Part 2 law says clicks decide, which would keep the orphan. **I am amending it: structural canonicality overrides clicks when the loser sits on a path pattern that exists for no other product.** A live `/product/porta-cabin/` implies a fifteenth cluster that we do not have, and leaving it teaches Google a URL shape we will spend years unteaching. **Redirect the orphan into the canonical.** The 301 carries the signal, so the canonical inherits the combined 21,270 impressions — this consolidates, it does not discard.

### 2.3 Self-slug and cross-cluster strays
| Retire | 301 → | Why |
|---|---|---|
| `/product/porta-cabins/porta-cabins` | `/product/porta-cabins` | Self-slug, 9 clicks. Codex 410'd 20 synthetic self-slugs; this one is live with traffic, so it redirects rather than 410s. |
| `/product/porta-cabin-house` | `/product/prefabricated-houses/porta-cabin-house` | 144 clicks on 590 impressions versus the canonical's 841 on 141,427. Clear duplicate of a top-ten page. |
| `/product-category/porta-cabins` | `/product/porta-cabins` | 117 clicks, 15,651 impressions competing with the hub's 1,471 and 163,606. A category page has no job the hub does not do better. |

### 2.4 The price-blog swamp — seven pages fighting for one query
`porta cabin price` and `porta cabin cost` are currently split across seven URLs totalling 484 clicks. None can rank properly while the other six exist.

**Winner: `/porta-cabin-cost`** — 125 clicks, cleanest URL, carries no year that will date.

| Retire | Clicks |
|---|---|
| `/porta-cabin-price-a-complete-guide-2025` | 95 |
| `/porta-cabin-price-a-complete-guide-2024` | 88 |
| `/porta-cabin-cost-per-square-foot` | 61 |
| `/porta-cabin-price-in-india` | 56 |
| `/porta-cabin-costs-2024-guide` | 46 |
| `/porta-cabin-office-price` | 13 |

All 301 → `/porta-cabin-cost`, which is then rewritten to cover per-square-foot, by-size and by-grade pricing in one place and to link the hub as the buying route. **`/porta-cabin-price-in-delhi` (445 clicks) is KEPT** — that is local intent, a different query, and it is the second-best performer in the whole cluster.

### 2.5 Budget-bracket pages — retire the three thin ones only
`under-1-lakh` (207), `under-2-lakhs` (70) and `under-3-lakhs` (75) hold real, distinct budget demand and are **KEPT**. `under-4-lakhs` (10), `under-5-lakhs` (20) and `under-6-lakhs` (19) are thin templates with 49 clicks between them — **301 → `/porta-cabin-cost`**.

---

## 3. WHAT I AM DELIBERATELY NOT TOUCHING YET

**The terrace pair.** `/porta-cabin-on-terrace-cost` (1,050 clicks) and `/porta-cabin-on-terrace` (559) are your third-largest traffic source in this cluster — 1,609 clicks between them. They do compete, but redirecting 559 clicks on a hunch is reckless when the alternative is free: differentiate them, one on cost and one on feasibility, permissions and structural loading, and measure at the 23 August checkpoint. **If they still trade positions on one query then, the weaker merges.** Traffic this size gets evidence, not assumption.

**Trailing-slash duplicates.** Every URL in the export appears twice, with and without a slash, and both accrue traffic — the hub alone is split 804/667. This is site-wide, not C-01, and it is already ticketed as CI8. It should run before or alongside this event so we are not redirecting one variant and leaving the other live.

---

## 4. TOTALS

| | Count |
|---|---|
| URLs retired | **22** |
| Clicks consolidated onto winners | **826** |
| C-01 live pages after this event | **13** (from 14) |
| Terms abandoned | **zero** |

Every retired keyword becomes a supporting keyword on its absorbing page. That is the whole point: you keep targeting every term your customers use, but each term is owned by exactly one URL.

---

## 5. CODEX WORK ORDER

1. Implement all 22 redirects as **single-hop 301s**. No chains. Any existing redirect that now points at a retired URL is repaired to point at the final winner.
2. Repair every internal link that currently resolves to a retired URL so it points directly at the winner. **Zero internal links may resolve through a 301 after this event.**
3. Remove the retired URLs from the sitemap; the sitemap carries only live 200 canonicals.
4. Delete the orphaned source files and archive them byte-for-byte, exactly as in the index-hygiene event.
5. Report before-and-after: live 200 count, sitemap count, internal links resolving through redirects, and a hop-count check proving every redirect terminates in one step.
6. Preview, report to Fable 5, **STOP**. No merge without my approval.

**Copy I still owe for this event:** the supporting-keyword H3 for `portacabin office` on the Porta Cabin Office page, and the rewritten `/porta-cabin-cost` guide that absorbs six pages. Both come in Drop 3 with the remaining §H copy.
