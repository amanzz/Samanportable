# T6.12 — Flexible Photo Panel (no blank space) — Content Draft v1
**Date:** 10 Jul 2026 · **Author:** Fable 5 · **Ticket:** SHIKHAR T6.12
**Status:** Owner-approved when SAMAN issues the T6.12 build prompt naming this file.

## 1. The only new copy (verbatim)
Photo caption chip: `Manufactured at our Bengaluru & Greater Noida factories`
Alt text: `Insulated sandwich panels manufactured at a SAMAN Portable factory`

## 2. Design rule
The panels card (currently the shorter column) gains a photo panel between the rate
table's footnote and the CTA bar: a local panel/factory image (distinct from the
header-band crop), object-cover, rounded to match card inner radius, with the caption
chip overlaid bottom-left (mist chip, forest text). The photo panel FLEX-GROWS to
absorb all remaining vertical space so both cards end at the same line at ≥1024px.
If future content changes make the cabins card the shorter one, the same treatment
applies there. Below 1024px (stacked cards) the photo renders at a fixed 160px height.

## 3. Locks
No other text changes — everything byte-identical to T6.11. Image local WebP ≤40 KB,
lazy, explicit dims, zero CLS.
