# T6.7 — Group A Row Images + Wall/Roof Sheet Rows — Content Draft v1
**Date:** 10 Jul 2026 · **Author:** Fable 5 · **Ticket:** SHIKHAR T6.7
**Status:** Owner-approved when SAMAN issues the T6.7 build prompt naming this file.

## 1. Group A row thumbnails
PUF Panels and Sandwich Panels rows get 112×112 local WebP thumbnails (≤15 KB, lazy,
explicit dims) — same treatment as Group B rows. Source: local panel imagery already
in the repo (the header-band panel photo and/or page-structure/panel-images assets).
Use visibly DIFFERENT crops/compositions for the two rows. Line icons retired.

## 2. New rows — Wall Sheets and Roof Sheets (visible now, linked later)
Add two rows to Group A, after Sandwich Panels:
- `Wall Sheets` — thumbnail if a suitable local sheet image exists, else the Layers icon.
- `Roofing Sheets` — same rule.
Both rows are NOT links (no anchor element at all). Each carries a small pill tag
right of the name, verbatim: `Launching soon`
(Pill style: mist background, forest text, DS radius — same family as the chips.)
When /product/wall-sheet and /product/roofing-sheet return 200, a follow-up ticket
converts these rows to links and removes the tags.

## 3. Locks
No other changes. All existing copy byte-identical. The two row names + tag text
above are the only new strings. No hrefs added (explicitly: no "#" placeholders —
dead anchors are banned). Rate table, chips, footnotes untouched.
