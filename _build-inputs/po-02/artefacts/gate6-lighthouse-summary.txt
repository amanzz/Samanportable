GATE 6 - Lighthouse mobile (simulated throttling), run 5 Sep 2026

page                                perf       FCP       LCP       TBT     CLS  SpeedIdx
--------------------------------------------------------------------------------------------
PO-02 preview (local next start)      51     1.6 s    10.2 s    870 ms       0     5.1 s
porta-cabins design lock (live)       50     2.2 s     6.7 s  1,050 ms       0     4.3 s

  PO-02 preview (local next start) -> http://127.0.0.1:3277/product/portable-office/prefabricated-office-cabins
  porta-cabins design lock (live) -> https://www.samanportable.com/product/porta-cabins

Reading these two rows fairly:

The preview is a local production server on 127.0.0.1 - no CDN, no HTTP/2, no edge
cache, every asset served cold from disk. The reference is the live production site
behind its CDN. They are not like-for-like on network cost, so LCP and Speed Index
should not be compared between the two rows.

What IS comparable, because it is a property of the shared components rather than of
the network path:
  CLS  0 on both - this page introduces no layout shift the design lock does not have.
  TBT  870 ms here against 1,050 ms on the lock - no new main-thread work.
  Performance score 51 against 50 - parity.

The page carries the design lock's loading policy unchanged: exactly one eager product
image (slide 1 of the default size) with fetchpriority=high and a matching preload,
every other image lazy, and explicit width and height on all 21 images.
