# SEO Issue 9 Hygiene Fix - 2026-07-02

## Executive Summary

Issue #9 was reviewed for HSTS, `llms.txt`, Semrush bot, and crawlability hygiene.

Safe code/file change made:

- Added `public/llms.txt`.

No repo-level HSTS change was made because the audit includes `www`, apex, and a `blog.samanportable.com` PDF. A Next.js header would only partially cover app-served routes and would not reliably fix apex/domain forwarding or the WordPress/media subdomain. HSTS should be configured at the CDN/server layer.

No Semrush bot or blog crawlability code change was made because no repository rule blocks SemrushBot, Googlebot, or Bingbot, and live probes returned HTTP 200.

Final recommendation: PASS for `llms.txt`; MANUAL ACTION REQUIRED for HSTS; NO CODE ACTION for Semrush/crawlability at this time.

## Files Changed

- `public/llms.txt`
- `reports/link-issues/SEO_ISSUE_9_HYGIENE_FIX_20260702.md`

`public/sitemap.xml` was regenerated by `npm run build` and intentionally excluded from the commit.

## llms.txt Status

Audit source:

- `www.samanportable.com_llms.txt_not_found_20260701.csv`
- Affected URL: `https://www.samanportable.com/llms.txt`
- Reported status: 404

Action:

- Added a short factual `public/llms.txt`.
- Included site name, legal company reference already used on the site, brief business description, main product categories, website URL, sitemap URL, and contact email.
- Avoided fake claims, unverifiable claims, and promotional exaggeration.

Local verification:

- `http://localhost:3138/llms.txt`: HTTP 200
- Content type: `text/plain; charset=UTF-8`

## HSTS Status

Audit source:

- `www.samanportable.com_no_hsts_support_20260701.csv`
- Affected rows: 3

Affected URLs:

- `https://www.samanportable.com/`
- `https://samanportable.com/`
- `https://blog.samanportable.com/wp-content/uploads/2025/09/porta-office-cabin_Price-guide-2025.pdf`

Repo inspection:

- `next.config.js` has security headers such as `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, and `X-XSS-Protection`.
- No `Strict-Transport-Security` header is currently configured in repo.
- App-level Next.js headers would cover app-served routes only.
- The HSTS audit includes apex and a `blog.samanportable.com` PDF, so a repo-only fix would be incomplete.

Live header spot checks:

- `https://www.samanportable.com/`: HTTP 200, no `Strict-Transport-Security` header observed.
- `https://samanportable.com/`: 308 to `https://www.samanportable.com`, no `Strict-Transport-Security` header observed in checked response chain.
- Blog PDF: HTTP 200 `application/pdf`, no `Strict-Transport-Security` header observed.

Safe decision:

- HOLD repo code change.
- HSTS should be configured in Cloudflare/DigitalOcean/server/CDN so it applies consistently to apex, `www`, and the blog/media host.

### HSTS Manual Action Checklist

Recommended location: Cloudflare or the production edge/CDN that handles both apex and `www`. The blog/media subdomain must be included in the review before enabling subdomain coverage.

1. Confirm HTTPS works without certificate warnings for:
   - `https://www.samanportable.com/`
   - `https://samanportable.com/`
   - `https://blog.samanportable.com/`
   - Representative media/PDF URLs on `blog.samanportable.com`.
2. Confirm all HTTP requests redirect to HTTPS for apex, `www`, and blog.
3. Add HSTS without preload first:
   - Header: `Strict-Transport-Security: max-age=31536000`
4. Do not add `preload` in this phase.
5. Treat `includeSubDomains` as risky until every active subdomain is inventoried and HTTPS-stable.
6. If all subdomains are confirmed HTTPS-stable and owner accepts the risk, move later to:
   - `Strict-Transport-Security: max-age=31536000; includeSubDomains`
7. Re-crawl the three affected URLs after Cloudflare/CDN propagation.

## Semrush Bot Status

Audit source:

- `www.samanportable.com_semrush_bot_blocked_20260701.csv`
- Rows: 267
- Pattern: `https://www.samanportable.com/blog?page=32` through high blog pagination URLs, including `page=100` to `page=298`.

Repo inspection:

- `public/robots.txt` allows all crawlers:
  - `User-agent: *`
  - `Allow: /`
- No repo rule was found blocking `SemrushBot`.
- No repo rule was found blocking `Googlebot`.
- No repo rule was found blocking `Bingbot`.
- `src/middleware.ts` has a staging-only basic-auth/noindex gate controlled by `STAGING_GOOGLE_BLOCK=1`; production is a no-op when that env var is unset.

Live bot header spot checks:

- `SemrushBot` user agent on `/blog?page=100`: HTTP 200
- `Googlebot` user agent on `/blog?page=100`: HTTP 200
- `bingbot` user agent on `/blog?page=100`: HTTP 200

Conclusion:

- No code change recommended.
- The Semrush audit likely reflects crawler rate limiting, WAF behavior, stale crawl state, or Semrush-specific access conditions outside the repo.
- Do not weaken security rules without edge/WAF evidence.

## Googlebot/Bingbot Status

- `robots.txt` does not block Googlebot or Bingbot.
- Live `Googlebot` and `bingbot` user-agent probes returned HTTP 200 for a representative blog pagination URL.
- No repo-side action needed.

## Couldn't-Crawl-Page Status

Audit source:

- `www.samanportable.com_we_couldn`t_crawl_the_page_20260701.xlsx`
- Rows: 1
- Affected URL: `https://www.samanportable.com/blog?page=299`

Current behavior:

- Live URL returns HTTP 200.
- The page is intentionally out-of-range blog pagination.
- Current SEO behavior is:
  - canonical: `https://www.samanportable.com/blog`
  - robots: `noindex, follow`
  - hreflang `en-IN`: `https://www.samanportable.com/blog?page=299`
  - hreflang `x-default`: `https://www.samanportable.com/blog?page=299`

Conclusion:

- No code change recommended.
- Do not make deep blog pagination indexable.
- Treat this as safe/no-action unless a fresh crawl shows Googlebot/Bingbot cannot access valid URLs.

## Lint and Build Result

- `npm run lint`: PASS
- `npm run build`: PASS

Build note:

- `public/sitemap.xml` was regenerated by `next-sitemap`.
- It was not part of the intended issue #9 change and should not be staged.

## Final Recommendation

PASS for the safe repo fix:

- `llms.txt` added and locally verified.

MANUAL ACTION REQUIRED for HSTS:

- Configure at Cloudflare/DigitalOcean/server/CDN after subdomain inventory.
- Do not use preload yet.
- Use `includeSubDomains` only after confirming every active subdomain is HTTPS-stable.

NO CODE ACTION for Semrush bot and couldn't-crawl page:

- No repo-side Semrush/Googlebot/Bingbot block found.
- Live representative bot probes returned HTTP 200.
- `/blog?page=299` is intentionally noindex and canonicalized to `/blog`.
