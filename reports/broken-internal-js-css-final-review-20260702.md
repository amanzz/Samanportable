# Broken Internal JS/CSS Final Review - 2026-07-02

## Git Status Summary

The worktree is dirty with many unrelated existing changes. The scoped files for this stale Next.js chunk fix are:

- `src/lib/cacheHeaders.ts`
- `src/pages/product.tsx`
- `src/pages/product-category/[slug].tsx`
- `public/sitemap.xml`
- `reports/broken-internal-js-css-fix-20260702.md`
- `reports/broken-internal-js-css-final-review-20260702.md`

There are staged files unrelated to this review:

- `src/data/wp-export/posts/2nd-hand-containers.json`
- `src/data/wp-export/products/container-houses.json`

## Generated Folder Check

No generated build folder is tracked according to `git ls-files`, including:

- `.next/`
- `.next-js-css-fix-20260702/`
- `.next-*-verify/`
- `.next-batch*-main-preflight/`

Untracked generated verification/build folders are present locally, including `.next-js-css-fix-20260702/` and multiple `.next-*-verify/` folders. They must not be staged or committed.

## Diff Summary

Code changes are limited to dynamic HTML response caching:

- `src/lib/cacheHeaders.ts`: adds `setNoStoreCache()`.
- `src/pages/product.tsx`: applies `setNoStoreCache(res)` inside `getServerSideProps`.
- `src/pages/product-category/[slug].tsx`: replaces `setPublicEdgeCache(res)` with `setNoStoreCache(res)` inside `getServerSideProps`.

The change does not modify shared static asset headers. `next.config.js` still keeps `/_next/static/(.*)` on `public, max-age=31536000, immutable`.

## Sitemap Review Summary

`public/sitemap.xml` was regenerated by `postbuild`.

After the requested fresh `npm run build`, sitemap comparison against `HEAD` showed:

- Old URL count: 427
- New URL count: 448
- Removed URL count: 0
- Added URL count: 21

The 21 added URLs are container-office city pages already present in the dirty worktree. No important live URLs were removed after the final build.

## Build And Checks

- `npm run build`: passed.
- `npm run type-check`: passed.
- `npm run lint`: passed with no ESLint warnings or errors.

Build output noted the existing Browserslist/caniuse-lite freshness warning only.

## Stale Chunk Search Result

Search of active source/output (`.next`, `public`, `src`, `next.config.js`, `package.json`) found no stale chunk references.

Repository-wide search excluding generated build folders found matches only in:

- the original CSV report
- `reports/broken-internal-js-css-fix-20260702.md`

This final review report intentionally avoids repeating the exact stale filenames so it does not create a new false-positive match.

## Final Recommendation

Safe to commit/deploy the scoped stale-chunk fix changes only.

Do not use a broad `git add .` because the worktree contains unrelated staged/unstaged changes and untracked generated `.next*` folders. Stage only the intended files, and keep all generated build folders out of git.
